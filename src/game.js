class Game {
    constructor(canvas, context, player) {
        this.canvas = canvas;
        this.context = context;
        this.player = player;
        this.gameOver = false;
    };

};

export default Game;